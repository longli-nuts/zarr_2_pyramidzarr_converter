name: zarr-pyramid
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - boto3
  - dask
  - fsspec
  - numpy<2
  - rasterio
  - rioxarray
  - s3fs
  - xarray>=2024.10
  - zarr<3
  - pip:
      - ndpyramid
